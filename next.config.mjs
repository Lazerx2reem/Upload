/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["motionarray.imgix.net"], // Add the external image domain here
  },
  reactStrictMode: true,
};

export default nextConfig;
